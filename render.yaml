services:
  - type: web
    name: cheshiretoday
    env: python
    plan: free
    buildCommand: |
      cd frontend
      yarn install
      yarn build
    startCommand: |
      cd backend
      uvicorn server:app --host 0.0.0.0 --port $PORT

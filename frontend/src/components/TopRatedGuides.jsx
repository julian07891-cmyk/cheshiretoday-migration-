import React from "react";

/**
 * Guides are temporarily disabled until non-Amazon affiliate accounts are live.
 * Keep this component as a stub so existing imports don’t break.
 */
export default function __DISABLED_GUIDES_COMPONENT__() {
  return null;
}
